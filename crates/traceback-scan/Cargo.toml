[package]
name = "traceback-scan"
version = "0.1.0"
edition = "2024"
description = "Filesystem scanner for TraceBack"
license = "MIT"

[dependencies]
globset = "0.4"
thiserror = "2.0"
time = { version = "0.3", features = ["formatting"] }

[dev-dependencies]
tempfile = "3.23"
